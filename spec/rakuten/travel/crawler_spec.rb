RSpec.describe Rakuten::Travel::Crawler do
  it "has a version number" do
    expect(Rakuten::Travel::Crawler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
